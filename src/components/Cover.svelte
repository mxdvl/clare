<style>
	.cover {
		background-color: var(--cover);
		border: var(--line) solid var(--text);
		border-top-right-radius: 2rem;
		border-bottom-right-radius: 2rem;

		min-height: 48rem;
		position: relative;

		grid-area: page;
		margin: -1rem;
		margin-bottom: -1.5rem;
	}

	h1 {
		position: absolute;
		right: 4rem;
		bottom: 4rem;
		color: var(--cover);
		text-shadow: 2px 2px 0 #fefefe40, -2px -2px 0 var(--text);
	}
</style>

<div class="cover">
	<h1>WORDS BY CW</h1>
</div>
